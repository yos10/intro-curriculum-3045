doctype html
html(lang="jp")
  head
    meta(charset="UTF-8")
    title アンケート
  body
    h1 どちらが食べたいですか？
    form(method="post", action=path)
      span 名前: 
      input(type="text", name="name")
      input(type="radio", id="firstItem", name="favorite", value=firstItem checked)
      label(for="firstItem") #{firstItem} 
      input(type="radio", id="secondItem", name="favorite", value=secondItem)
      label(for="secondItem") #{secondItem} 
      button(type="submit") 投稿
